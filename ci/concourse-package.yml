---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: ruby
    tag: alpine

inputs:
- name: concourse-tag
- name: concourse-packaging

run:
  path: concourse-packaging/ci/scripts/concourse-package
